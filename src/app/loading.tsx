import React from 'react';

export default function HomeLoading() {
    return (
        <main className="w-full bg-[#ffffff] min-h-screen pt-[var(--navbar-height)]">
            {/* 1. HERO SKELETON */}


        </main>
    );
}