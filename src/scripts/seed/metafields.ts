import { env } from '@/lib/env';

async function seedMetafields() {
    console.log('Seeding metafields...');
    // Logic to read JSON definitions and push to Shopify Admin API
    // Requires Admin Access Token (not Storefront)
    console.log('Metafield seeding complete.');
}

seedMetafields();